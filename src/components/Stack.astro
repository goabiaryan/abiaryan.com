---
import { stack } from "../data/site";

interface Props {
  compact?: boolean;
}

const { compact = false } = Astro.props;
---

<aside class:list={["stack", compact && "stack-compact"]} aria-label="The inference stack">
  <p class="label">The stack I teach</p>
  <ol>
    {
      stack.map((item, i) => (
        <li>
          <span class="n">{String(i + 1).padStart(2, "0")}</span>
          <div>
            <span class="layer">{item.layer}</span>
            {
              !compact && (
                <ul class="chips">
                  {item.tools.map((tool) => (
                    <li>{tool}</li>
                  ))}
                </ul>
              )
            }
          </div>
        </li>
      ))
    }
  </ol>
</aside>
